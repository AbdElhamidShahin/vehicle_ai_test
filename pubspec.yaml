# pubspec.yaml — المُحدَّث
name: vehicle_ai_test
description: "تسجيل لوحات السيارات — Deepgram Nova-2 Real-time"
publish_to: 'none'

version: 1.0.0+4

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ الميكروفون كـ PCM Stream مباشر
  record: ^6.1.2

  # ✅ WebSocket يشتغل على Android + iOS + Web
  web_socket_channel: ^3.0.3

  # باقي الـ dependencies
  http: ^1.5.0
  path_provider: ^2.1.5
  share_plus: ^12.0.2
  geolocator: ^14.0.2
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true